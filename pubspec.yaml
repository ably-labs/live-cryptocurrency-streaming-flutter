name: ably_cryptocurrency
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  ably_flutter_plugin: ^1.0.0+dev.2
  get_it: ^5.0.1
  http: ^0.12.2
  intl: ^0.16.1
  syncfusion_flutter_charts: ^18.3.52
  tweet_ui:
    git:
      url: https://github.com/escamoteur/tweet_ui
      ref: add_overflow_to_RetweetInformation
  twitter_api: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
